{
  "id": "720e15fa",
  "type": "chore",
  "state": "done",
  "summary": "scripts/backfill-cost.ts: compute cost_usd for existing queries rows from model_pricing; one-shot operator script",
  "source": {
    "type": "human"
  },
  "created_at": "2026-04-30T12:20:10.178Z",
  "updated_at": "2026-04-30T12:51:24.658Z",
  "files": [
    {
      "path": "scripts/backfill-cost.ts"
    }
  ],
  "tags": [
    "billing",
    "backfill",
    "operator"
  ],
  "relationships": {
    "parent": "0d91fca3"
  },
  "description": "scripts/backfill-cost.ts — one-shot operator script for existing queries rows.\n\nLive data so far: 9 OpenRouter requests, ~$0.167 total (DeepSeek 7 / Sonnet 2). All have model_used, input_tokens, output_tokens, but cost_usd will be NULL after child 1's migration.\n\nAlgorithm:\n  SELECT id, model_used, input_tokens, output_tokens, created_at\n  FROM queries\n  WHERE cost_usd IS NULL\n    AND model_used IS NOT NULL\n    AND input_tokens IS NOT NULL\n    AND output_tokens IS NOT NULL;\n\nFor each row: cost_usd = computeCost({ modelId: model_used, inputTokens, outputTokens, cachedInputTokens: 0, at: created_at }).  Update the row.\n\nLimitation: cached_input_tokens isn't recoverable for historical rows (we never logged it).  Backfill uses 0 for the cached-token count, so backfilled cost slightly over-estimates true cost for cache-using historical Sonnet calls.  Acceptable for the current 2 Sonnet rows (both pre-tier-pro, no cache hits expected).\n\nAfter backfill:\n- SUM(cost_usd) FROM queries should be within $0.01 of OpenRouter's reported $0.167.\n- Post any remaining diff to ops chat / log it; spec is 'never recompute cost', so no second-pass adjustment.\n\nDone when:\n- ts script that runs via 'tsx scripts/backfill-cost.ts' (or 'npm run backfill-cost')\n- Idempotent: cost_usd IS NOT NULL rows are skipped on re-run\n- Documented: README routine for 'after deploying child 5, run this once on prod'\n- Manual: SUM(cost_usd) ≈ OpenRouter reported total",
  "resolution": {
    "commit": "HEAD",
    "resolved_at": "2026-04-30T12:51:24.658Z",
    "resolved_by": "unknown",
    "note": "Backfill script scans queries with NULL cost_usd, computes cost from model_used + tokens + pricing-at-created_at, writes back. cached_input_tokens=0 (historical rows don't have it). Skips rows whose model has no pricing row instead of aborting. Run via 'npm run backfill-cost'."
  }
}