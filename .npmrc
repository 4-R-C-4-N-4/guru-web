# Supply-chain hardening (todo:8ee0d414)
# save-exact: `npm install <pkg>` writes an exact version, never a ^range —
# package.json stays the single pinned source of truth alongside the lockfile.
save-exact=true
# engine-strict: fail fast if the Node version drifts from what CI/VPS run.
engine-strict=true
