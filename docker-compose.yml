services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: guru
      POSTGRES_USER: guru
      POSTGRES_PASSWORD: guru_dev
    ports:
      - "5432:5432"
    volumes:
      - guru_pgdata:/var/lib/postgresql/data

volumes:
  guru_pgdata:
